
  {% extends 'base.html.twig' %}

    {% block content %}
     <!-- Navigation -->
        {{ include ('layout/menu.html.twig') }}

    <!-- Header -->
    <header class="masthead">
      <div class="container d-flex h-100 align-items-center">
        <div class="mx-auto text-center">
          <h1 class="mx-auto my-0 text-uppercase">Pop them up</h1>
          <h2>
            <ul>
           
        </ul>
          </h2>
        </div>
      </div>
    </header>
     <!-- Projects Section -->
    <section id="projects" class="projects-section bg-light">
    <div class="container">
    <h3>Liste des figurines</h3>
     <div class="row">
             {% for toy in toys %}
              <div class="col-lg-4 col-md-6 mb-4">
              <div class="card h-100">
                <a href="{{ path('detail', {'toyId': toy.id}) }}" ><img class="card-img-top" src="http://placehold.it/700x400" alt=""></a>
                <div class="card-body">
                  <h4 class="card-title">
                    <a href="{{ path('detail', {'toyId': toy.id}) }}"> {{ toy.name|upper}} </a>
                  </h4>
                  <h5>{{ toy.getPrice()}} €</h5>
                  <p class="card-text">{{ toy.getDescription() }}</p>
                </div>
                <div class="card-footer">
                  <small class="text-muted">&#9733; &#9733; &#9733; &#9733; &#9734;</small>
                </div>
              </div>
            </div>
            {% endfor %}
           
          </div>
    </div>
     
      
    </section>
    
    {% endblock %}
